CREATE TABLE m_user(
	id varchar(32) NOT NULL PRIMARY KEY,
	name varchar(30) NOT NULL COMMENT '名字'
) COMMENT '用户';

CREATE TABLE m_user_age (
	id varchar(32) NOT NULL PRIMARY KEY,
	user_id VARCHAR(32) NOT NULL COMMENT '用户ID',
	age INT(3) NOT NULL COMMENT '年龄'
) COMMENT '用户年龄';